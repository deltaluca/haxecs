package hello;

import nape.geom.Vec2;

typedef A = Int;

class World {
    static var a:Int = 10;

	static function main():Void {
        var xs = [0,1,2,3];
        for(i in 0...10) {}
        for(i in xs) {}
	}
}
